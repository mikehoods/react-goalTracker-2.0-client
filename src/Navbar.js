const Navbar = () => {
    return (
        <nav className="navbar">
            <h1>Goal Tracker</h1>
            <div className="links">
                <a href="/">Home</a>
                <a href="/create">Add New Goal</a>
            </div>

        </nav>
    );
}
 
export default Navbar;